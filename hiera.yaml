---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data
hierarchy:
  - name: "version"
    path: "version.yaml"

  - name: "OS family"
    path: "os/%{facts.os.family}.yaml"

  - name: "common"
    path: "common.yaml"
